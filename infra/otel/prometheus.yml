global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']
  - job_name: 'teamos-backend'
    metrics_path: '/health/support-bundle' # Or custom prometheus path
    static_configs:
      - targets: ['backend:3000']
